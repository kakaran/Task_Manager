import Login from "../Pages/Login/Login";

const CommonRoutes = [{ path: "/", element: <Login /> }];

const AdminRoutes = [{ path: "/dashboard" }];

const EngineerRoutes = [];

const Routes = () => {
  return;
};

export default Routes;
